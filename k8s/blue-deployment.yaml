apiVersion: apps/v1
kind: Deployment
metadata:
  name: petclinic-blue
spec:
  replicas: 2
  selector:
    matchLabels:
      app: petclinic
      version: blue
  template:
    metadata:
      labels:
        app: petclinic
        version: blue
    spec:
      containers:
      - name: petclinic
        image: rohith13s/petclinic:IMAGE_TAG
        ports:
        - containerPort: 8080

